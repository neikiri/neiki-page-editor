/**
 * SelectionManager — saves, restores, and transforms iframe selections/ranges.
 * Stub for Task 1. Full implementation in Task 2.
 */
export class SelectionManager {
  constructor(/* canvasManager */) {}
  getSelection() { return null; }
  getRange() { return null; }
  saveRange() {}
  restoreRange() { return false; }
  wrapSelection(/* tag, attrs */) {}
  unwrapSelection(/* tag */) {}
}

export default SelectionManager;
